// src/commands/support.ts
import { Context, InlineKeyboard } from 'grammy'

export const handleSupport = async (ctx: Context) => {
  // Сначала отвечаем на callback query, чтобы убрать загрузку с кнопки
  await ctx.answerCallbackQuery()
  
  const supportMessage = `🔧 *Служба поддержки Lou Quiz*\n\n` +
    `Если у вас возникли вопросы, проблемы с оплатой или работой приложения, ` +
    `вы всегда можете обратиться к нашему специалисту поддержки.\n\n` +
    `👨‍💻 Напишите: @matsa0111\n\n` +
    `Мы поможем вам решить любые возникшие вопросы:\n` +
    `• Проблемы с доступом к тестам\n` +
    `• Технические неполадки\n` +
    `• Вопросы по оплате\n` +
    `• Другие вопросы по работе приложения\n\n` +
    `⏱ Время работы поддержки: ежедневно с 9:00 до 21:00\n\n` +
    `💡 Для более быстрого решения вашего вопроса, пожалуйста, ` +
    `опишите проблему максимально подробно.`

  // Отправляем сообщение с поддержкой форматирования Markdown
  await ctx.reply(supportMessage, {
    parse_mode: 'Markdown',
    // Добавляем кнопку для быстрого перехода в чат поддержки
    reply_markup: new InlineKeyboard()
      .url('Написать в поддержку', 'https://t.me/matsa0111')
  })
}